<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SingleCard" parent="AppTheme"></style>
    <style name="SingleCard.Header" parent="SingleCard"></style>
    <style name="SingleCard.Description" parent="SingleCard.Header"></style>
    <style name="SingleCard.Phone" parent="SingleCard.Description"></style>

</resources>
